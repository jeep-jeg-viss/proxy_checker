[project]
name = "proxy-checker-backend"
version = "0.1.0"
description = "FastAPI backend for proxy checker"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "requests>=2.31",
    "pydantic-settings>=2.6",
]
